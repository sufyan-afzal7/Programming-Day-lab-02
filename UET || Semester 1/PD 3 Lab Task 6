#include <iostream>
using namespace std;
main()
{

  double MB;
  double b;

  cout << "Enter the size in megabytes (MB): ";
  cin >> MB;
	b = MB * 8388608;
  cout << MB << " MB is equivalent to " << b << " bits.";

}
